apiVersion: v1
kind: Service
metadata:
  name: auth-service-db
  namespace: merchprint
  labels:
    app: auth-service-db
spec:
  selector:
    app: auth-service-db
  ports:
    - name: mongo
      port: 27017
      targetPort: 27017
  type: ClusterIP
