apiVersion: apps/v1
kind: Deployment
metadata:
  name: companies-service-db
  namespace: merchprint
  labels:
    app: companies-service-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: companies-service-db
  template:
    metadata:
      labels:
        app: companies-service-db
    spec:
      containers:
        - name: mongo
          image: mongo:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              value: companies_user
            - name: MONGO_INITDB_ROOT_PASSWORD
              value: companies_pass
          volumeMounts:
            - name: data
              mountPath: /data/db
      volumes:
        - name: data
          emptyDir: {}
