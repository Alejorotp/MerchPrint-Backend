apiVersion: v1
kind: Service
metadata:
  name: companies-service-db
  namespace: merchprint
  labels:
    app: companies-service-db
spec:
  selector:
    app: companies-service-db
  ports:
    - name: mongo
      port: 27017
      targetPort: 27017
  type: ClusterIP
