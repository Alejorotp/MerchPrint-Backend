apiVersion: apps/v1
kind: Deployment
metadata:
  name: events-service
  namespace: merchprint
  labels:
    app: events-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: events-service
  template:
    metadata:
      labels:
        app: events-service
    spec:
      containers:
        - name: events-service
          image: merchprint/events-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: RMQ_URL
              valueFrom:
                secretKeyRef:
                  name: rmq-secret
                  key: RMQ_URL
            - name: DB_URI
              valueFrom:
                secretKeyRef:
                  name: events-service-secret
                  key: DB_URI
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 20
