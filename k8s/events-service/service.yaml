apiVersion: v1
kind: Service
metadata:
  name: events-service
  namespace: merchprint
  labels:
    app: events-service
spec:
  selector:
    app: events-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
